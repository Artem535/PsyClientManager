diff --git a/lib/include/oclero/qlementine/animation/WidgetAnimationManager.hpp b/lib/include/oclero/qlementine/animation/WidgetAnimationManager.hpp
index 37dd199..76abcb4 100644
--- a/lib/include/oclero/qlementine/animation/WidgetAnimationManager.hpp
+++ b/lib/include/oclero/qlementine/animation/WidgetAnimationManager.hpp
@@ -5,6 +5,7 @@
 
 #include <oclero/qlementine/animation/WidgetAnimator.hpp>
 
+#include <memory>
 #include <unordered_map>
 #include <optional>
 
@@ -32,6 +33,7 @@ namespace oclero::qlementine {
 class WidgetAnimationManager {
 public:
   WidgetAnimationManager();
+  ~WidgetAnimationManager();
 
   bool enabled() const;
   void setEnabled(bool enabled);
@@ -68,6 +70,7 @@ private:
   QEasingCurve _focusEasingCurve;
   QEasingCurve _defaultEasingCurve;
   QEasingCurve _linearEasingCurve;
+  std::shared_ptr<bool> _alive{ std::make_shared<bool>(true) };
   std::unordered_map<const QWidget*, WidgetAnimator*> _map;
 };
 } // namespace oclero::qlementine
diff --git a/lib/src/animation/WidgetAnimationManager.cpp b/lib/src/animation/WidgetAnimationManager.cpp
index 5a2cab2..fa0473b 100644
--- a/lib/src/animation/WidgetAnimationManager.cpp
+++ b/lib/src/animation/WidgetAnimationManager.cpp
@@ -8,6 +8,12 @@ WidgetAnimationManager::WidgetAnimationManager() {
   initializeEasingCurves();
 }
 
+WidgetAnimationManager::~WidgetAnimationManager() {
+  // Some widgets, notably QCalendarWidget internals, may emit deferred
+  // destruction callbacks after the style teardown has already started.
+  *_alive = false;
+}
+
 bool WidgetAnimationManager::enabled() const {
   return _animationsEnabled;
 }
@@ -40,7 +46,11 @@ void WidgetAnimationManager::addWidget(const QWidget* widget, WidgetAnimator* wi
   if (!findWidget(widget)) {
     _map.insert_or_assign(widget, widgetAnimator);
 
-    QObject::connect(widget, &QObject::destroyed, widget, [this, widget]() {
+    const auto alive = _alive;
+    QObject::connect(widget, &QObject::destroyed, [this, alive, widget]() {
+      if (!*alive) {
+        return;
+      }
       removeWidget(widget);
     });
   }
