import { Calendar } from "../widgets/calendar.slint";

export component MainWindow inherits Window {
    width: 548px;
    height: 600px;
    Text {
        text: "Hello world";
        color: green;
    }
}