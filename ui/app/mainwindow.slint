import { Calendar, ICalendarLogic } from "@widgets/calendar.slint";

export {ICalendarLogic}

export component MainWindow inherits Window {
    width: 548px;
    height: 600px;
    in-out property <int> test;

    calendar := Calendar { }
}
